---
galaxy_info:
  role_name: pip
  author: marcus_grant
  description: Ensure pip is available, install pip and pipx packages, configure shell integration.
  issue_tracker_url: https://github.com/marcus-grant/ansible-collection-dotfiles/issues
  license: GPL-2.0-or-later

  min_ansible_version: '2.1'

  platforms:
    - name: Debian
      versions:
        - bookworm
    - name: Archlinux
      versions:
        - all

  galaxy_tags:
    - dotfiles
    - dots
    - pip
    - pipx
    - python
    - shell

dependencies: []
